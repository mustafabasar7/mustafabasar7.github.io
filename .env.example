# DeepSeek API key — used server-side only by /api/agent (never exposed to the browser).
# Set this as an environment variable on your host (e.g. Vercel project settings),
# NOT with a VITE_ prefix. Without it, the agents fall back to doc-grounded canned demos.
DEEPSEEK_API_KEY=your_deepseek_api_key_here
